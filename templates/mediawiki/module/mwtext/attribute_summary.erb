<% groups(attr_listing, "Attribute") do |list, name| %>
  === <%= name %> <small><a href="#" class="summary_toggle">collapse</a></small> ===
  <ul class="summary">
    <% list.each do |meth| %>
      <%= yieldall :item => meth %>
    <% end %>
  </ul>
<% end %>
